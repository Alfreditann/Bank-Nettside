<a class="floating-credentials-button" href="/credentials" aria-label="Open credentials page" title="Open credentials page">
  <span>✦</span>
</a>
<style>
  .floating-credentials-button {
    position: fixed;
    top: 18px;
    right: 18px;
    width: 56px;
    height: 56px;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    background: linear-gradient(135deg, #00c6ff, #66e3ff);
    color: #001b2e;
    font-size: 1.2rem;
    font-weight: 900;
    box-shadow: 0 16px 32px rgba(0, 198, 255, 0.28);
    z-index: 1000;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .floating-credentials-button:hover {
    transform: translateY(-2px) scale(1.04);
    box-shadow: 0 20px 38px rgba(0, 198, 255, 0.35);
  }

  .floating-credentials-button:focus-visible {
    outline: 3px solid rgba(255, 255, 255, 0.9);
    outline-offset: 3px;
  }
</style>
